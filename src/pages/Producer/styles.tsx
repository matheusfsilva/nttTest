import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const BoxButton = styled(Box)({
    display: 'flex',
    justifyContent: 'flex-end',
    marginTop: 15
})

export const BoxBody = styled(Box)({

}
)

export const BoxBodySx = {
    marginTop: { sm: '100px', xs: '30px' }
}

export const paperSx = {
    // marginTop: '10px',
    display: 'flex',
    padding: '24px',
    marginTop: 'auto'
}