import { Paper } from "@mui/material";
import { styled } from '@mui/material/styles';

export const PaperCardChart = styled(Paper)({
    padding: '24px',
    marginBottom: '10px',
    borderRadius: '15px',
})